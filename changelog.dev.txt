Development commit log, auto-appended by .claude/hooks/post-commit-changelog.sh
(via .git/hooks/post-commit). Curated, user-facing release notes live in
changelog.txt — this file records internal churn and is not published.

v5
- add pnpm dev static server for examples with SSE live reload, wired as a ROLLUP_WATCH-gated rollup plugin so plain builds never open a socket (2026-07-30)
- dedupe coincident shape vertices before gap-fill triangulation, fixing doubled and shape-crossing gap triangles at exact contact and the red octagon certification tests (2026-07-28)
- orient octagons flat-to-flat for an exact truncated-square tiling and render gap fill beneath primary shapes, add coverage and corner-square certification tests (2026-07-28)
- point repository metadata at the IDontKnowMyUsername fork: package.json repository/bugs/homepage, security advisory link, contributing links, license banner, readme releases link (2026-07-28)
- validate worker color-function args, pin coverage denominator, widen stale-dist guard, cross-check d.cts exports, scope attw suppression to the worker entrypoint, fix culori and node-20 doc drift (2026-07-28)
- implement the gamut mapping and dedup fallback plan: CSS Color 4 output mapping, boundary-straddle tiling tests (2026-07-27)
- implement the color pipeline plan: culori backend, wide-gamut output, scale quantization (2026-07-27)
- speed up tiling vertex dedup and shape gap-fill classification, add color pipeline plan (2026-07-27)
- fix per-shape allocation guard and harden rendering, serialization, worker, CI, and docs (2026-07-27)
- sync pnpm-lock.yaml with @types/chroma-js runtime dependency (2026-07-26)
- fix canvas halo divergence with explicit strokeColor, deep-freeze colorbrewer, cap point allocations, make shape dispatch exhaustive, add fuzz/error-path/ESM-entry tests, add ESM/TypeScript examples, harden postbuild and lint scripts, SHA-pin and extend CI (all-branch triggers, multi-OS, CodeQL, dependency review, coverage summary, publish tag guard) (2026-07-26)
- fix coverage measurement (inlineSources + single-bundle collection), deep-validate palettes/colors/fromData, add public API JSDoc and Point/Centroid exports, fix hexagon honeycomb spacing and poisson point-count invariant, add v4-to-v5 migration guide and rewrite v5 release notes (2026-07-26)
